# ENKI TRADING PLATFORM — STATE 2026-04-20 (END OF SESSION)

## THIS SESSION ACCOMPLISHED

### Ready to deploy (pending user PowerShell push)
1. **Alpha detCat date-argument fix** — 3 callers (afCalc L2050, afSubmit L2078, editSave L2173) were omitting the date param, causing D2 V2 trades to save as `c:'M'` instead of M1-M4. Fixed in `/mnt/user-data/outputs/alpha_dashboard_v2.html`. Smoke-tested: D2 $11.39 on 2026-04-14 → M4 ✓, $2.50→M1, $5→M2, $8→M3, pre-cutoff→M.
2. **Alpha allocation schema** — 6 edits applied (C bracket added, B price range fixed, AVOID emptied, SC2 list updated, V1 Agenda C/B split fixed, OA_REF mirror updated). Shipped at commit `3774525`.
3. **Alpha AVOID warning disabled** — Line 2026 hardcoded `av = false`. Omega retains AVOID warning.

### Research + validation (no live code changes yet)
4. **Frozen-pipeline formula reverse-engineered**: hybrid rule
   - Aggregates (n, wr, mn, tn, tg, tar, stop_hits) from `rn` field (net, post-fee)
   - Extremes (aw, al, pf, max_win, max_loss) from `rg` field (gross, pre-fee)
   - Kelly = `wr − (1−wr) / (aw/|al|)` × 100, qK = Kelly / 4
5. **Starting equity**: $3000 for both Alpha and Omega (DAY[0] is post-first-trade equity)
6. **recompute_module.js built and validated** — 251 lines, standalone:
   - SS: 6/6 setups × 17 stats = 102/102 exact match
   - CS: 15/15 keys, 165/165 stats exact
   - SF: 6/6 setups, all nested categories match
   - MO: 34/34 months × 4 stats exact
   - DAY equity: 470/470 exact (computed via Method A compound from $3000 start)
   - DAY dd: 470/470 exact
   - DAY sma: 470/470 within ±5 (my "20-day SMA of cumulative rs" ≈ Python formula)
   - DAY r20/pos: different formula from Python pipeline, cannot reverse-engineer exactly
7. **Python's banker's rounding** implemented in JS (exact parity — was causing 86.25→86.2 vs 86.3 edge case)
8. **Delivered**: `/mnt/user-data/outputs/recompute_module.js`

## STAGE PLAN

**Stage 1** (done) — Validate recompute formula against frozen blobs
**Stage 2a** (done) — Build standalone recompute module, unit-test against frozen
**Stage 2b** (next session, requires approval) — Wire module into Alpha:
   - Inject recompute_module.js code as inline `<script>` in alpha_dashboard_v2.html
   - Replace `SS`, `SF`, `CS`, `MO`, `ML`, `DAY` frozen JSON consts with `const ALL = AT.concat(newTrades); const {SS,SF,CS,MO,ML,DAY,latest} = recomputeStats(ALL, 3000);`
   - Add "last ticker" footer to each of 6 tabs using `result.latest`
   - Trigger recompute on save/edit/delete (currently page-load only)
**Stage 2c** — Mirror to Omega (same change, startingEquity=3000)

## FORMULA DRIFT WARNING (Stage 2b commit message note)

When live, `r20` and regime classification (Above/AbovePlus/Below) will show **different values for historical days** compared to current frozen pipeline. Equity curve itself will look identical. SMA line will look essentially identical. Only the Above/AbovePlus/Below color-coded pill at the top of the Equity tab will change for some days. Acceptable trade-off since the original Python indicator formula isn't recoverable from the HTML artifacts and trying to exactly match it is diminishing returns.

## ORPHANED DATA

3-4 D2 trades already saved to Firebase with `c:'M'` (pre-detCat-fix):
- SKYQ on 4/13, D2 test on 4/14, possibly others

Options (user chooses):
A) Delete manually in Trade Log → re-add
B) I write a one-time migration that reads `/alpha/new_trades`, recomputes correct category for any D2 trade with `c:'M'` + date ≥ 2026-04-13, and writes back

## STILL PENDING (prior backlog)

1. Apply Forecast tab to Alpha (from prior session)
2. Setup × Cycle heatmap on Omega Analytics
3. Hub Omega card stale stats (showing 820/76.6%/1373 vs correct 818/77.1%/1254)
4. Position Calculator "Send to Omega" button
5. Alpha Fee Monitor tab empty state
6. Alpha overnight fees modeling when broker data arrives

## KEY DATA POINTS LOCKED

- Alpha: 881 trades, WR 81.27%, Sum NetR 74.929, Sum AccR 1845.26%, Start $3000, Peak/Final $9.79B, 470 trading days, 30% fee + stop-hit cap, Method A compound
- Omega: 818 trades, WR 77.1%, Sum NetR 41.39, Sum AccR 1254.22%, Start $3000, Peak/Final $297M, 414 trading days, 20% flat fee, Method A compound
- Cutoff: 2026-04-13 pre=V1, on/after=V2. Alpha AVOID list empty. Omega AVOID retained.

## REPO/DEPLOY

Live repo: `C:\Users\besht\Desktop\enki-trading\`
Alpha live file: `alpha/index.html` served at `/alpha/`
Alpha source work dir: `C:\Users\besht\Desktop\AI Projects\Alpha and Omega\Alpha\`

Deploy flow (PowerShell):
```powershell
cd C:\Users\besht\Desktop\enki-trading
Copy-Item "C:\Users\besht\Desktop\AI Projects\Alpha and Omega\Alpha\alpha_dashboard_v2.html" ".\alpha\index.html" -Force
git add alpha/index.html
git commit -m "message"
git push
```

## PERMANENT RULES

1. Shelf detection: `\bel\b` regex (never substring match)
2. Avoid detection: `isAvoid = (t.signal === 'avoid')` for V2 only (Omega). Alpha AVOID disabled entirely.
3. Date parsing: direct part extraction, never `new Date("YYYY-MM-DD")` (UTC+2 bug)
4. Allocation cutoff: Apr 13 2026 · pre=V1 · on/after=V2
5. Alpha fee: 30% with stop-hit cap at -1.0, Method A compound
6. Omega fee: 20% flat, Method A compound
7. Stats formula: aggregates use rn, extremes use rg
8. Banker's rounding for stats parity
9. Starting equity: $3000 for both Alpha and Omega
10. CSS in panes: isolated `<style>` blocks
11. Module scope: `window.X = X` pattern
12. Template literals when HTML has single-quotes
13. Firebase rejects undefined → use destructure-omit + `sanitizeForFirebase()`
14. Light theme default all platforms
15. Alpha repo layout: live is `alpha/index.html` at `/alpha/`
16. `detCat(setup, entry, dateStr)` — all callers MUST pass dateStr
